# 智能旅行规划师 — 产品介绍与开发纪实

## 一、产品概述

**智能旅行规划师**是一款基于 AI 的一站式旅行规划工具。用户只需输入出发地、目的地、出行日期和个人偏好，系统即可在 1-2 分钟内生成包含每日行程、住宿推荐、餐饮建议、交通方案、费用预估和天气提醒的完整旅行计划。

与市面上的旅行规划工具不同，本产品的核心理念是：**AI 做决策，真实数据做支撑**。AI 负责行程编排和创意推荐，高德地图等真实数据源负责交通路线、POI 信息和地理校验，二者协同确保方案既有创意又可执行。

### 产品定位

- **目标用户**：不想花大量时间做攻略、但又希望行程质量高的旅行者
- **核心价值**：从"我想去旅行"到"拿着方案出发"，全程 AI 辅助，5 分钟搞定
- **使用场景**：周末短途、假期长线、说走就走、多人拼团等各类出行

---

## 二、核心功能

### 2.1 智能表单 — 8 步收集用户偏好

用户通过分步表单提供出行信息，每一步都经过精心设计：

| 步骤 | 内容 | 亮点 |
|------|------|------|
| 基础信息 | 出发地、目的地、日期 | 支持"我没想好去哪"模式，细分为"周边转转""飞一趟""长线深度""自然为主""城市为主"五个子选项 |
| 同行人 | 独行/情侣/亲子/朋友/商务 | 影响行程节奏和住宿推荐 |
| 节奏 | 紧凑/适中/慢旅行 | 控制每天安排密度 |
| 兴趣 | 历史人文/自然风景/美食/购物等 | 多选，影响景点类型 |
| 住宿 | 经济/中档/高端/民宿/青旅等 | 支持多种风格偏好（如"设计师民宿""小众有格调"） |
| 餐饮 | 当地特色/不辣/清真/素食等 | 包含饮食禁忌 |
| 预算 | 总预算金额 | AI 据此调整酒店档次和活动选择 |
| 特殊需求 | 必去/必避/无障碍等 | 自由文本输入 |

**日期模式三选一**：
- **固定日期**：选定出发和返程日，AI 严格按天数安排，不会建议改日期
- **大概知道**：选月份和大致天数，AI 灵活推荐
- **完全不确定**：可输入文字提示（如"想五一前后出去"），AI 自行判断

### 2.2 AI 方案生成 — 双方案对比

系统同时生成 **两套完整方案**（标准模式），供用户对比选择：

- **方案 A**：可能偏经济实惠、公共交通为主
- **方案 B**：可能偏自由灵活、不同路线组合

也可选择**极速模式**，30-60 秒出一套方案，适合赶时间的场景。

每套方案包含：
- 每日详细行程（时间、活动、地点、时长、费用）
- 交通方案总览（高铁/飞机/自驾的整体路线说明）
- 住宿推荐（含价格、优缺点、区域说明）
- 美食推荐（餐厅名、菜系、人均价格、评分）
- 推荐景点列表（含门票、开放时间）
- 费用明细（交通/住宿/餐饮/景点/其他，分项合计）
- 实用贴士（如当地注意事项、穿衣建议）

### 2.3 天气预报与穿衣建议

自动获取目的地未来 16 天天气预报（基于 Open-Meteo），在行程中嵌入：
- 每日最高/最低温度
- 天气状况图标
- **穿衣建议**（如"建议穿薄外套，昼夜温差较大"）
- **是否带伞**（降水概率超过 40% 提醒携带）

### 2.4 互动地图

基于高德地图 JS API 的可交互路线地图：
- 标注每日行程中的关键地点
- 显示城市间的移动路线
- 支持缩放和拖动
- 点击标记可查看地点详情

### 2.5 AI 对话修改方案

生成方案后，用户可以通过聊天窗口与 AI 对话修改：

- **直接修改**："把第 3 天的博物馆换成逛街""预算降到 3000""不想在北京玩"
- **先聊再改**："第一天住宿有没有其他推荐？""这个景点值得去吗？"
- AI 会根据指令智能判断是"先对话"还是"直接执行修改"
- 修改后方案实时更新，费用自动重算，版本号递增

### 2.6 方案导出

支持三种导出格式：
- **图片（PNG）**：适合分享到社交平台
- **PDF**：适合打印携带
- **Markdown**：适合二次编辑

### 2.7 历史方案管理

- 每次生成的方案自动保存到本地（localStorage）
- 登录后同步到云端（Supabase）
- 可查看、删除历史方案

---

## 三、技术架构

### 3.1 技术栈总览

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端框架 | Next.js 14 (App Router) | SSR + 客户端渲染 |
| UI | React + TypeScript + Tailwind CSS | 组件化开发 |
| AI 引擎 | 阿里云通义千问 (qwen-turbo) | 旅行方案生成与修改 |
| 地图服务 | 高德地图 JS API + Web Service API | 地图展示、地理编码、POI 搜索、公交路线 |
| 天气服务 | Open-Meteo API | 未来 16 天天气预报 |
| 数据库 | Supabase (PostgreSQL) | 用户认证与方案存储 |
| 本地存储 | localStorage | 免登录方案暂存 |

### 3.2 数据流架构

```
用户填写表单
    ↓
前端提交到 /api/generate
    ↓
┌─────────────────────────────────┐
│  第一阶段：AI 生成方案框架       │
│  · 通义千问生成完整 JSON 方案    │
│  · 含行程、住宿、费用等全部内容  │
└──────────────┬──────────────────┘
               ↓
┌─────────────────────────────────┐
│  第二阶段：真实数据补充          │
│  · 高德地图搜索 POI / 交通路线   │
│  · Open-Meteo 获取天气预报       │
│  · 高德/千问联网查询列车班次     │
│  · 交通信息校验与纠正            │
└──────────────┬──────────────────┘
               ↓
┌─────────────────────────────────┐
│  第三阶段：数据规范化            │
│  · JSON 结构标准化               │
│  · 日期/天数对齐                 │
│  · 异常数据修复                  │
│  · 交通描述常识校验              │
└──────────────┬──────────────────┘
               ↓
SSE 流式返回给前端 → 渲染方案页面
```

### 3.3 项目规模

- **源代码文件**：64 个（.ts / .tsx / .css）
- **总代码量**：约 8,200 行
- **核心模块**：37 个 TypeScript 模块 + 26 个 React 组件

---

## 四、开发历程 — 从零到一的全过程

### 第一阶段：基础搭建（Day 1-2）

**目标**：搭建项目框架，跑通最小 MVP

**做了什么**：
- 使用 Next.js 14 + TypeScript + Tailwind CSS 初始化项目
- 设计分步表单 UI（8 个步骤的信息收集）
- 接入阿里云通义千问 API，实现基础的方案生成
- 前端渲染生成结果（每日时间线、费用、景点列表）

**遇到的困难**：
- AI（通义千问）返回的 JSON 格式不稳定，经常包含 markdown 标记、多余文字或格式错误
- 长行程（7 天以上）生成时间过长，前端等待超时

**怎么解决的**：
- 编写了多层 JSON 解析器（`lib/qwen.ts`），逐步尝试：直接解析 → 提取 JSON 块 → 修复截断 → 修复常见格式错误
- 引入 SSE（Server-Sent Events）流式响应机制，在 AI 生成期间发送心跳 ping 保持连接不中断

---

### 第二阶段：地图与地理信息（Day 3-4）

**目标**：在方案中加入可交互地图，展示路线

**做了什么**：
- 集成高德地图 JS API，绘制行程路线地图
- 实现地理编码（地名 → 经纬度），标注每日行程点
- 实现城市间路线连线

**遇到的困难**：
- 地理编码失败率高：AI 生成的地点名有时不规范（如"鼓浪屿沙滩"无法直接编码）
- 地图标记点错位：有的景点被定位到了完全不相关的城市（如北京的景点标到了甘肃）

**怎么解决的**：
- 实现多级地理编码回退（`lib/amap-geocode-resolve.ts`）：完整地名 → 去后缀重试 → 加城市限定重试 → 仅用城市名
- 为每个 POI 标签附加 `cityHint`（城市提示），地理编码时优先在该城市范围内搜索
- 替换地图上的技术术语（"POI""走廊""直线示意"等）为用户友好的中文描述

---

### 第三阶段：真实数据接入 — 告别 AI 编造（Day 5-7）

> 这是整个开发过程中最关键、最困难的阶段。

**目标**：解决 AI 编造数据的核心问题——虚假的车次号、不存在的酒店、离谱的价格

**做了什么**：
- 建立**三阶段数据管线**：AI 生成框架 → 高德/千问补充真实数据 → 后处理校验
- 接入高德 Web Service API：
  - POI 搜索（酒店、景点、餐厅的真实信息）
  - 公交/铁路路线规划（站点、时长、换乘）
  - 地理编码（精确坐标）
- 接入千问联网搜索功能，作为高德查不到时的补充手段
- 实现交通信息后处理（`lib/post-enrich-transit.ts`）：
  - 识别高铁/火车段，用高德公交路线规划替换 AI 编造的车次
  - 识别飞机段，链接到携程航班查询
  - 高德查不到时，用千问联网搜索"北京到上海的高铁"获取参考信息
- 实现交通描述常识校验（`lib/transport-sanity.ts`）：自动检测并修正不合理的交通描述

**遇到的困难**：

1. **AI 编造车次号**：生成的"G123""K456"等车次号完全是捏造的
   - 解决：不再让 AI 填写车次号，改为从高德公交路线 API 获取真实铁路信息；实在查不到的，提示"请在 12306 查询"

2. **住宿推荐不靠谱**：AI 推荐的酒店可能不存在或价格离谱
   - 解决：用高德 POI 搜索目的地城市的真实酒店，结合用户偏好（如"设计师民宿""高端"）筛选；价格来源标注为"仅供参考"

3. **交通时长不合理**：AI 说"景德镇到乌镇大巴 2 小时直达"（实际不可能）
   - 解决：编写交通常识校验模块，检测城市间交通方式和时长是否合理，发现不合理自动修正

4. **数据来源不一致**：高德返回的站名格式与 AI 生成的不一致
   - 解决：建立统一的数据规范化流程，所有数据经过 `normalizeItinerary` 标准化

5. **API 调用太多导致速度慢**：每个城市要查 POI、交通、天气，多个城市串行查询
   - 解决：使用 `Promise.allSettled` 并行请求，高德 POI + 天气 + 交通同时发起

---

### 第四阶段：AI 对话修改方案（Day 8-9）

**目标**：让用户能通过自然语言对话修改已生成的方案

**做了什么**：
- 实现 `/api/plan-chat` 端点，支持对话式方案修改
- 设计"先对话再执行"的交互模式：AI 判断用户意图是聊天讨论还是直接修改
- 实现增量更新（delta output）：AI 只返回修改过的天，未改的天用 `_keep:true` 标记，大幅减少输出量
- 实现前后端方案合并逻辑：将 AI 的增量修改与原方案合并

**遇到的困难**：

1. **AI 不听指令**：用户说"不想在北京玩"，AI 反问"你想换成哪里？"不执行
   - 解决：在提示词中明确列出所有"动作词"（换成/去掉/不去/不想/删掉/取消……），遇到这些词直接执行修改不反问
   - 增加"模糊指令规则"：用户说"换成其他地方"时，AI 必须自己决定替换什么，不允许追问

2. **AI 嘴上说改了但实际没改**：回复"好的，已帮你调整"但 `planModified` 为 false
   - 解决：增加"防矛盾规则"——如果 `assistantMessage` 包含"已经""帮你"等完成时措辞，`planModified` 必须为 true

3. **长行程修改 JSON 被截断**：16 天方案的 JSON 太大，超出 token 限制被截断
   - 解决：引入 delta 输出 + 输入压缩。修改时只传精简版方案（去掉 webNote/pros/cons 等冗余字段），AI 只输出改动部分

4. **费用没跟着更新**：用户说"预算加到 5000"，行程改了但费用表没变
   - 解决：在提示词中增加强制规则——任何行程变更都必须重算 `costBreakdown`

---

### 第五阶段：体验打磨与鲁棒性（Day 10-12）

**目标**：全面提升用户体验，修复边界情况，为发布做准备

**做了什么**：
- 天气预报优化：修复城市名拼接错误（如"重庆市·北京"），穿衣建议文案优化
- "周边转转"模式强化：严格限制推荐范围在 3 小时车程内，不允许跨省远途
- 各开放模式细化："飞一趟 3-5 天""长线深度""自然为主""城市为主"各有独立的约束规则
- 前端输入防护：所有文本框加 `maxLength`，防止超长输入
- localStorage 防爆：写入前检查容量，超限时自动清理最旧的方案
- 前端超时保护：5 分钟无响应自动中止请求
- 友好错误提示：JSON 解析失败不再显示技术错误，而是"AI 返回数据不完整，请重试"
- 生产日志清理：非开发环境不输出 `console.log`

**遇到的困难**：

1. **JSON 解析频繁失败**：AI 输出过长被截断，导致 JSON 不完整
   - 解决：实现多级 JSON 修复（`truncateToLastComplete` 智能截断到最后一个完整键值对），3 次不同策略的修复尝试

2. **生成速度太慢**：标准模式有时 2-3 分钟，用户以为卡死了
   - 解决：
     - 动态 token 预算（短行程少、长行程多，但有上限）
     - 动态超时（根据 token 量计算，而非固定值）
     - 极速模式跳过后处理环节（省 10-30 秒）
     - 并行化千问联网搜索（最多 3 个同时查询）
     - 提示词中要求长行程精简输出（每天 2-3 条活动，描述从简）

3. **前端页面加载闪白**：Next.js 服务端与客户端渲染不一致
   - 解决：清除 `.next` 缓存，确保组件 hydration 一致

---

### 第六阶段：AI 行为精调（Day 13-至今）

**目标**：让 AI 对话修改更智能、更可靠

**做了什么**：
- 增加"彻底移除"规则：用户说"不想在 XX 玩"时，必须移除该城市所有观光日，不留一天
- 使用 `useRef` 解决 React 闭包导致的陈旧状态问题：确保每次聊天请求使用最新的方案数据
- 增加服务端调试日志：记录 AI 原始输出、前端传入的方案版本、合并后的每日主题

**正在解决的问题**：
- AI 对"彻底移除某城市"的执行力还不够，需要反复验证
- 前端状态同步需要进一步确认（useRef 方案刚上线）

---

## 五、项目数据一览

| 指标 | 数据 |
|------|------|
| 源代码文件数 | 64 个 |
| 总代码行数 | ~8,200 行 |
| React 组件数 | 26 个 |
| API 路由数 | 5 个（generate / plan-chat / trips / trips/[id] / amap/geocode） |
| 核心工具模块 | 37 个 |
| 外部 API 集成 | 4 个（通义千问、高德地图、Open-Meteo、Supabase） |
| 表单收集步骤 | 8 步 |
| 方案导出格式 | 3 种（PNG / PDF / Markdown） |
| 开发周期 | ~13 天（持续迭代中） |

---

## 六、技术难点总结

### 最难的 5 个问题

1. **AI 数据真实性**
   - 问题：AI 会编造不存在的车次、酒店、景点
   - 方案：建立"AI 生成框架 + 真实数据回填"的双层架构，用高德地图 API 和千问联网搜索作为数据源，AI 只负责编排和创意

2. **JSON 稳定性**
   - 问题：AI 输出的 JSON 格式不稳定，长文本经常被截断
   - 方案：多层解析修复 + 动态 token 管理 + 输出精简规则 + 增量输出

3. **AI 对话控制**
   - 问题：AI 不按指令执行，该改不改、该聊不聊
   - 方案：精细化提示词工程，明确列出触发修改的动作词、防矛盾规则、模糊指令处理规则

4. **性能平衡**
   - 问题：要在"数据完整"和"生成速度"之间找平衡
   - 方案：动态 token/超时管理、并行 API 调用、极速模式跳过非关键步骤、按天数分级精简规则

5. **地理数据准确性**
   - 问题：地图标点跑偏、城市间交通信息不合理
   - 方案：多级地理编码回退 + 交通常识校验 + 城市提示限定

---

## 七、后续优化方向

### 近期（优先级高）

#### 1. 自驾游模式
- **当前状态**：基础支持，但路线规划不够细致
- **需要做的**：
  - 接入高德驾车路线 API，获取精确的公路路线、里程和预估耗时
  - 生成每段驾车的详细路线说明（走哪条高速、在哪个出口下）
  - 添加沿途服务区、加油站推荐
  - 每日驾车时长控制（建议不超过 4-5 小时）
  - 高速过路费估算
  - 沿途景观公路推荐（如独库公路、川藏线等经典自驾路线）

#### 2. 摩托车旅行模式
- **当前状态**：完全未支持
- **需要做的**：
  - 摩托车专属路线规划（避开禁摩路段和高速公路）
  - 骑行距离和时间估算（考虑摩托车平均速度）
  - 骑行装备清单推荐
  - 沿途加油站标注（摩托车油箱小，需要更频繁加油）
  - 适合摩托车的风景公路推荐
  - 天气对骑行的影响评估（雨天、大风提醒）
  - 摩旅社区热门路线参考

#### 3. AI 对话修改完善
- 提高"彻底移除某城市"的执行一致性
- 支持更复杂的修改指令（如"把前 3 天和后 3 天对调""加一天自由活动"）
- 历史对话上下文更智能的管理

### 中期

#### 4. 部署上线
- 选择国内云服务商（阿里云/腾讯云/Vercel 国内加速）
- 配置域名和 HTTPS
- 生产环境性能优化（代码分割、静态资源压缩、CDN）
- 监控和报警（API 调用量、错误率、响应时间）

#### 5. 实时数据增强
- 接入景点门票实时价格（携程/美团等平台）
- 接入航班实时价格和余票信息
- 接入 12306 实时列车信息（如有可靠 API）
- 住宿价格从 OTA 平台获取实时数据（而非 AI 估算）

#### 6. 社交与分享
- 方案分享链接（无需登录即可查看）
- 方案评论和评分
- 热门方案榜单
- 用户行程游记（旅行结束后分享实际体验）

### 远期

#### 7. 多模态能力
- 根据风景照片推荐类似目的地（"我想去这种地方"）
- AI 生成旅行海报/封面图
- 语音输入修改方案

#### 8. 行中助手
- 实时导航对接
- 行程进度追踪
- 突发情况应对（航班取消、景点临时关闭等）
- 每日行程推送提醒

#### 9. 商业化
- 酒店/机票预订跳转（联盟佣金）
- 高级功能会员制（更多方案对比、更详细推荐）
- 旅行社/定制游 B2B 合作
- 目的地旅游局合作推广

---

## 八、开发反思

### 做对了的事

1. **SSE 流式传输**：避免了长请求超时的问题，用户能看到"正在生成"的反馈
2. **双层数据架构**：AI 做创意编排 + 真实 API 做数据校验，比纯 AI 可靠得多
3. **渐进式开发**：先跑通 MVP，再逐步加入地图、天气、对话修改等功能
4. **以用户反馈驱动迭代**：每一轮修改都基于真实测试反馈，不是凭空猜测

### 踩过的坑

1. **过度信任 AI 输出**：最初直接使用 AI 返回的交通信息，结果全是编造的。教训：AI 擅长创意和组织，但具体数据必须从可信源获取
2. **JSON 格式脆弱**：让 AI 返回结构化 JSON 本身就是个挑战，尤其是长文本。需要非常强的解析修复能力
3. **提示词工程比想象中难**：控制 AI 行为（什么时候聊天、什么时候执行、怎么处理模糊指令）需要极其精细的规则设计
4. **前端状态管理**：React 闭包问题导致异步操作使用了过期的状态数据，需要用 `useRef` 等手段规避

### 对 AI 辅助开发的感悟

这个项目本身就是一个"AI 辅助开发"的实践。整个 8,200 行代码的项目，从架构设计、代码编写、调试排错到文档撰写，全程由一人（产品方）与 AI（Cursor + Claude）协作完成。这种开发模式的核心价值在于：

- **产品方专注需求和体验**：不需要写代码，只需要清楚描述"我要什么"和"哪里不对"
- **AI 负责技术实现**：代码编写、API 集成、bug 修复、性能优化全部由 AI 完成
- **迭代速度极快**：发现问题 → 描述问题 → AI 修复 → 立即测试，一个循环通常在 5-10 分钟内完成

---

*最后更新：2026 年 4 月 2 日*
*项目状态：开发中，核心功能可用，持续优化迭代*
